# TeraSync Pharmacy Portal - Environment Variables

# API Configuration
# Local: use your machine IP. Production: use the deployed API URL
# NEXT_PUBLIC_API_BASE_URL=https://medsync-api-v1.up.railway.app/api/v1
# NEXT_PUBLIC_SOCKET_URL=https://medsync-api-v1.up.railway.app
# Local example (update IP to your machine):
NEXT_PUBLIC_API_BASE_URL=http://192.168.88.87:3000/api/v1
NEXT_PUBLIC_SOCKET_URL=http://192.168.88.87:3000

# Socket namespace – must be /patient-pharmacy for chat/order real-time updates
# Backend emits chat:message, order:new, order:updated, dispatch:updated to this namespace only
NEXT_PUBLIC_SOCKET_NAMESPACE=/patient-pharmacy

# Enable legacy event support during migration
# Set to "true" during migration, "false" after backend removes legacy events
NEXT_PUBLIC_ENABLE_LEGACY_EVENTS=false

# Google Maps API - Address autocomplete (signup and unauthenticated address fields)
# Used by: app/api/places/autocomplete (Next.js route). Same key as backend GOOGLE_MAPS_API_KEY
# is fine. Required for address autosuggestion when user is not logged in (e.g. signup).
# When logged in, pharmacy also uses backend /geo/autocomplete (backend needs GOOGLE_MAPS_API_KEY).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Environment
NODE_ENV=development
